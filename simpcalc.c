#include <stdio.h>

int main () {
    float input1, input2, output;
    char operator;

    printf("Input a number: ");
    scanf("%f", input1);

    printf("Choose operator (+ - * or /): ");
    scanf("%c", operator);

    switch (operator) {
        case '': 
    }

    printf("Input 2nd number: ");
    scanf("%f", input2);

    printf("%f", output)
}